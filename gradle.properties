# ===========================================================================
#  FluxLogic — build properties
#  Target: Minecraft 26.2 "Chaos Cubed" (Java 25), Fabric toolchain.
#
#  Pin everything here so bumping a Minecraft drop is a one-file change.
#  Version numbers verified against the Fabric "Developing for 26.2" notes
#  (Loom 1.17, Gradle 9.5.1, Loader 0.19.3, API 0.153.0+26.2).
# ===========================================================================

# Gradle daemon — modern MC builds (decompile + mixin) want headroom.
org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true

# --- Minecraft / Fabric ----------------------------------------------------
minecraft_version=26.2
# 26.2 uses official Mojang mappings (the new no-remap Loom flow); there is
# no separate Yarn version to pin. See build.gradle (officialMojangMappings()).
loader_version=0.19.3
fabric_api_version=0.153.0+26.2

# --- Mod metadata ----------------------------------------------------------
mod_version=0.1.0
maven_group=com.fluxlogic
archives_base_name=fluxlogic

# Minimum Java for 26.2 is 25; toolchain enforces it in build.gradle.
java_version=25
